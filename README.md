# port-watchdog-alert
 Java application that scans a computer or network device to check which ports are open . It then tells you which ports might be risky or unsafe, based on what services they are used for
